'use client';
import { useEffect } from 'react';
import { useRouter } from 'next/navigation';

export default function Loading() {
  // This empty component will prevent the default loading UI from showing
  return null;
}