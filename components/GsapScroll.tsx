"use client";

import { useEffect } from "react";

// This component no longer uses Lenis for smooth scrolling
// It's a placeholder in case we need to add scroll-related features later
export default function GsapScroll() {
  // No scrolling effect
  return null;
}
